services:
  # Frontend Service (Static Site)
  - type: web
    name: coldbotx-frontend
    env: static
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
